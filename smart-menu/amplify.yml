version: 1
applications:
  - appRoot: smart-menu
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
      cache:
        paths:
          - node_modules/**/*